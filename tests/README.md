# Tests

Place all tests in this directory.

Organize tests to mirror the `src/` structure. Examples:

```
tests/
├── unit/           # Unit tests
├── integration/    # Integration tests
└── e2e/            # End-to-end tests
```

## Running Tests

> [Describe how to run the test suite for this project, e.g., `dotnet test`, `pytest`, `npm test`]
