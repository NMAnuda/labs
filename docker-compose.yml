version: '3.8'

services:
  # Example Database Service (PostgreSQL)
  # Un-comment and modify to match the project's actual database
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_USER: ${DATABASE_USER:-postgres}
  #     POSTGRES_PASSWORD: ${DATABASE_PASSWORD:-password}
  #     POSTGRES_DB: ${DATABASE_NAME:-madlabs_db}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

  # Example Cache Service (Redis)
  # cache:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
