# ------------------------------------------------------------------------------
# MADLABS Cloud Build CI/CD Boilerplate
# ------------------------------------------------------------------------------
steps:
  # 1. Install dependencies & Lint
  - name: 'node:20'
    entrypoint: 'npm'
    args: ['ci']
    dir: 'src'

  # 2. Run Tests
  - name: 'node:20'
    entrypoint: 'npm'
    args: ['test']
    dir: 'src'

  # 3. Build Docker Image (if pushing to GCP)
  # - name: 'gcr.io/cloud-builders/docker'
  #   args: ['build', '-t', 'gcr.io/$PROJECT_ID/my-service:$COMMIT_SHA', '.']

  # 4. Push Image
  # - name: 'gcr.io/cloud-builders/docker'
  #   args: ['push', 'gcr.io/$PROJECT_ID/my-service:$COMMIT_SHA']

  # 5. Deploy to Cloud Run
  # - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  #   entrypoint: gcloud
  #   args: ['run', 'deploy', 'my-service', '--image', 'gcr.io/$PROJECT_ID/my-service:$COMMIT_SHA', '--region', 'us-central1', '--platform', 'managed']

# images:
#   - 'gcr.io/$PROJECT_ID/my-service:$COMMIT_SHA'
